(in-package :it.bese.ucw)

;;; This file is dinamically compiled for each application instance.
;;; Each form is first eval'd and then compiled with js:js*. *context*
;;; is bound while compiling, so (context.application *context*) is
;;; the hosting application.

